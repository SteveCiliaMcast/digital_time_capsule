name: digital_time_capsule
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.0
  firebase_database: ^10.2.3
  flutter_map: ^6.0.0
  latlong2: ^0.9.0  # Required for handling coordinates
  lucide_icons: ^0.257.0
  image_picker: ^1.1.2
  permission_handler: ^11.4.0
  flutter_local_notifications: ^17.2.4
  geolocator: ^13.0.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
